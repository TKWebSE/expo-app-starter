# 実装記録

設計に基づく実装内容と、実装中に判明した事項を記録します。

## 記録する内容

- 実装順序
- 実装済み項目
- 技術上の判断
- 設計との差分
- 動作確認結果
- Finance Appでの試用結果
- スターターへ戻す改善
- 残作業

## 2026-07-24：実装開始

### 実装順序

1. Expo・TypeScript・Expo Routerの起動基盤
2. Formatter・ESLint・型検査・Vitest・GitHub Actions
3. 認証とプロフィールの入力スキーマ
4. Supabase接続、Repository境界、`profiles` Migration
5. 認証画面と認証状態によるルーティング
6. 設定・プロフィール画面、共通フィードバック

### 実装済み

- Expo SDK 57、React Native、TypeScript、Expo Routerの初期設定
- Android・Web共通の最小画面とWeb用404画面
- テーマトークンと薄いボタンラッパー
- Development・Staging・Production用の環境変数検証
- パスワード、表示名のZodスキーマと境界値テスト
- Supabase Auth・Profile Repositoryの境界
- `profiles` テーブル、RLS、更新日時、自動作成トリガー
- PR・main用の自動品質ゲート

### 技術上の判断

- Expoの現行安定版であるSDK 57系から開始した
- 新規登録時はメールアドレスの `@` より前を初期表示名にする
- クライアントから初期表示名をメタデータとして渡し、DBトリガーでもメールアドレスから補完する
- V1にはProfile Repositoryの削除APIとRLSのDELETEポリシーを作らない
- UIラッパーはReact Native標準部品から薄く開始し、実利用で不足したAPIだけを増やす

### 確認結果

- Formatter：成功
- ESLint：成功
- TypeScript型検査：成功
- 単体・Migration契約テスト：21件成功
- Web静的ビルド：成功

### 残作業

- 認証画面と認証状態のStore・Hook
- スプラッシュ、メール確認、パスワード再設定
- レスポンシブなホーム・設定・プロフィール画面
- Snackbar、ダイアログ、ローディング、空状態、エラー状態
- テーマ永続化と多言語化の土台
- Supabase CLIによるDB・RLS結合テスト
- Android実機とWeb幅別の手動確認

## 2026-07-24：認証画面とルーティング

### 実装済み

- 起動時の保存済みセッション復元と認証状態監視
- 未認証時はログイン、認証済み時はホームへ振り分け
- ログイン画面、新規登録画面、メール確認案内画面
- React Hook FormとZodによる項目単位の入力エラー
- 認証エラーを画面上部へ表示し、手動で閉じられるSnackbar
- 送信中の入力・ボタン無効化と二重送信防止
- ログイン成功後のホーム遷移とメールアドレス表示
- Web静的生成時にSupabaseのブラウザ用Storageへ触れない分岐

### TDDで追加した保証

- 保存済みセッションの有無に応じた認証状態
- ログイン成功・失敗時の状態
- ログイン処理の二重送信防止
- 新規登録後に確認対象メールアドレスを保持

### 確認結果

- Formatter：成功
- ESLint：成功
- TypeScript型検査：成功
- 単体・Migration契約テスト：27件成功
- Web静的ビルド：成功

### 次の実装候補

- 確認メール再送
- パスワード再設定と新しいパスワード設定
- ログアウト確認を含む設定画面
- プロフィール表示・更新画面

## 2026-07-24：V1画面・共通基盤の完成

### 実装済み

- 確認メール再送、パスワード再設定メール、新しいパスワード設定
- AndroidディープリンクとWeb URLからの再設定セッション復元
- 767px以下の下部ナビゲーションと768px以上の固定サイドバー
- 設定・プロフィール・ログアウト確認
- 端末設定／ライト／ダークの選択とAsyncStorageへの永続化
- Snackbar、確認ダイアログ、ローディング、空、エラーの共通部品
- Development／Staging専用のUI部品確認画面
- 日本語リソースを起点とした多言語化の土台
- Android設定検証と秘密情報パターン検査
- Supabase CLI向けprofiles Migration／RLS契約テスト

### 確認

- Formatter、ESLint、TypeScript、単体・契約テスト、Android設定、秘密情報検査、Web静的ビルドを品質ゲートとする
- Android実機、Web幅別、読み上げなどの端末依存項目はリリース前に手動確認する
- Android向け本番バンドル（1,535モジュール）の生成に成功。確認時点では接続端末・AVDがないため実機起動は未実施

## 2026-07-24：DB・RLS結合確認

- Supabase CLI 2.109.1とローカルDocker環境でMigrationを適用
- 実操作テストにより、`authenticated`へのテーブル権限付与漏れを検出・修正
- 本人プロフィールの参照・更新、他人プロフィールの非表示・更新拒否、DELETE権限なしを確認
- pgTAP結合テスト10件が成功

## 2026-07-24：V2要件確定・テーマ即時反映

- V2をアカウント削除、テーマ完成、認証強化、DB・品質ゲートの4領域に決定
- ライト・ダークのカラーパレットを追加
- Theme Providerから解決済みパレットを公開
- 画面、共通レイアウト、入力、ボタン、Snackbar、ダイアログ、状態表示を動的テーマへ移行
- テーマ選択時に再起動を待たず画面全体へ即時反映する

## 2026-07-24：V2認証・アカウント管理

### 実装済み

- 現在のパスワードとメールアドレス確認を伴うアカウント削除画面
- JWT利用者だけを削除するSupabase Edge Function
- `auth.users`削除時の`profiles`連動削除Migration
- 現在のパスワードによる再認証を伴うメールアドレス変更
- 確認メール再送の60秒クールダウンと残り秒数
- 無効・期限切れの再設定リンクから再送画面への復帰
- 予期しないセッション終了時の期限切れ通知
- GitHub ActionsでのSupabase起動、Migration、pgTAPテスト

### 結合確認

- V1・V2 MigrationからローカルDBを再構築
- RLS・所有者操作・DELETE禁止・プロフィール連動削除の11件が成功
- ローカル専用ユーザーでEdge Functionを呼び、認証ユーザーとプロフィールが削除されることを確認
